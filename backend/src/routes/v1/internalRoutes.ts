import { Router } from 'express';

const router = Router();

/**
 * @summary Internal routes placeholder
 * @description Authenticated API endpoints will be registered here
 */

export default router;
